"use client";

// import { useTipTap } from "@/components/layouts/TipTap/TipTap";
import { FontIcon as Icon } from "@/components/ui/icons";
import { MenuButton } from "@/components/layouts/TipTap/components/ui";

const Font: React.FC = () => {
  // const { editor } = useTipTap();

  return <MenuButton title="font" onClick={() => {}} Icon={Icon} />;
};

export default Font;
