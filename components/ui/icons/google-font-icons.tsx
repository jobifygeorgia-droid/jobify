import Icon from "./Icon";

export type IconT = {
  className?: string;
  filled?: boolean;
  size?: number;
};

export const Mail: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      mail
    </Icon>
  );
};

export const Location: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      location_on
    </Icon>
  );
};

export const Star: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      star
    </Icon>
  );
};

export const Search: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      search
    </Icon>
  );
};

export const Bag: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      business_center
    </Icon>
  );
};

export const Filter: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      page_info
    </Icon>
  );
};

export const Close: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      close
    </Icon>
  );
};

export const ArrowRight: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      east
    </Icon>
  );
};

export const DoubleArrowLeft: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      keyboard_double_arrow_left
    </Icon>
  );
};

export const DoubleArrowRight: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      keyboard_double_arrow_right
    </Icon>
  );
};

export const KeyboardArrowRight: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      keyboard_arrow_right
    </Icon>
  );
};

export const KeyboardArrowLeft: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      keyboard_arrow_left
    </Icon>
  );
};

export const Calendar: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      calendar_today
    </Icon>
  );
};

export const Wallet: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      account_balance_wallet
    </Icon>
  );
};

export const Heart: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      favorite
    </Icon>
  );
};

export const Doc: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      docs
    </Icon>
  );
};

export const Video: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      video_camera_back
    </Icon>
  );
};

export const AddVideo: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      video_camera_back_add
    </Icon>
  );
};

export const Image: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      image
    </Icon>
  );
};

export const Link: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      link
    </Icon>
  );
};

export const Phone: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      call
    </Icon>
  );
};

export const CV: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      article_person
    </Icon>
  );
};

export const Eye: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      visibility
    </Icon>
  );
};

export const EyeOff: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      visibility_off
    </Icon>
  );
};

export const Plus: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      add
    </Icon>
  );
};

export const Minus: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      remove
    </Icon>
  );
};

export const Notification: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      notifications
    </Icon>
  );
};

export const Warning: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      gpp_maybe
    </Icon>
  );
};

export const EducationPen: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      history_edu
    </Icon>
  );
};

export const EducationHat: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      school
    </Icon>
  );
};

export const University: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      account_balance
    </Icon>
  );
};

export const OptionsDots: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      more_horiz
    </Icon>
  );
};

export const Delete: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      delete
    </Icon>
  );
};

export const Edit: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      edit
    </Icon>
  );
};

export const Download: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      download
    </Icon>
  );
};

export const GridView: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      grid_view
    </Icon>
  );
};

export const ListView: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      view_stream
    </Icon>
  );
};

export const Login: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      login
    </Icon>
  );
};

export const Logout: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      logout
    </Icon>
  );
};

export const Person: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      person
    </Icon>
  );
};

export const Check: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      check
    </Icon>
  );
};

export const Exclamation: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      exclamation
    </Icon>
  );
};

export const Crown: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      fertile
    </Icon>
  );
};

export const BurgerMenu: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      menu
    </Icon>
  );
};

export const Home: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      home
    </Icon>
  );
};

export const FilterSecondary: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      tune
    </Icon>
  );
};

export const CalendarSecondary: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      calendar_month
    </Icon>
  );
};

export const Work: React.FC<IconT> = (props) => {
  const { className, filled, size } = props;

  return (
    <Icon className={className} filled={filled} size={size}>
      work
    </Icon>
  );
};
